version: "3.8"
services:
  phonics-h5:
    image: nginx:alpine
    container_name: phonics-h5
    restart: always
    ports:
      - "8081:80"
    volumes:
      - /volume1/web/phonics-h5:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

